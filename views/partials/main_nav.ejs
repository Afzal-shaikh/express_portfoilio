  <!--My Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">

     <!--Logo here -->
     <a aria-current="page" href="/home">
        <img src="assets\images\logo.png" width="40px" style = "object-fit: cover ;  height: 50%; width: 50%;" />
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">

        
     

        <li class="nav-item">
          <!--Navigate to Home -->
          <% if (title == "Home") { %>
            <a class="nav-link active" aria-current="page" href="/home">
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/home">
          <% } %> 
            <i class="fas fa-lg fa-home"></i> Home</a>
          </li>
          <!--Navigate to About Me -->
          <li class="nav-item">
            <% if (title == "About Me") { %>
              <a class="nav-link active" aria-current="page" href="/about">
            <% } else { %>
              <a class="nav-link" aria-current="page" href="/about">
            <% } %> 
              <i class="fas fa-lg fa-info-circle"></i> About Me</a>
          </li>
          <!--Navigate to Projects -->
          <li class="nav-item">
            <% if (title == "Projects") { %>
              <a class="nav-link active" aria-current="page" href="/projects">
            <% } else { %>
              <a class="nav-link" aria-current="page" href="/projects">
            <% } %> 
              <i class="fas fa-lg fa-project-diagram"></i> Projects</a>
          </li>
          <!--Navigate to Services -->
          <li class="nav-item">
            <% if (title == "Services") { %>
              <a class="nav-link active" aria-current="page" href="/services">
            <% } else { %>
              <a class="nav-link" aria-current="page" href="/services">
            <% } %> 
              <i class="fas fa-lg fa-cogs"></i> Services</a>
          </li>
          <!--Navigate to Contact Me-->
          <li class="nav-item">
            <% if (title == "Contact Me") { %>
              <a class="nav-link active" aria-current="page" href="/contact">
            <% } else { %>
              <a class="nav-link" aria-current="page" href="/contact">
            <% } %> 
              <i class="fas fa-lg fa-phone"></i> Contact Me</a>
          </li>

         
      </div>
    </div>
  </nav>
